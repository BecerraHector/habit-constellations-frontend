# URL base del backend. Vacio en desarrollo: vite hace proxy de /api al puerto 8080
# (ver vite.config.ts), asi el navegador no ve dos origenes y no hay CORS.
# En produccion se apunta al backend real, p.ej. https://api.tudominio.com
VITE_API_URL=
